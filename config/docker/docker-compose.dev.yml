services:
  bot-dev:
    build:
      context: .
      dockerfile: config/docker/Dockerfile
    container_name: tgtaps-support-bot-dev
    env_file:
      - .env
    volumes:
      - ./:/app
      - ./data:/app/data
    command: python -m app.main
