services:
  bot:
    build:
      context: .
      dockerfile: config/docker/Dockerfile
    container_name: tgtaps-support-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: python -m app.main
